[project]
name = "agents-multiagent-mcp"
version = "0.1.0"
description = "MCP server for the agents-multiagent workflow"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.9.0",
    "anthropic>=0.40.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
